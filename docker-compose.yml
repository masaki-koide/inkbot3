services:
  inkbot3:
    container_name: inkbot3
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Tokyo
    volumes:
      - inkbot3-data:/app/data

volumes:
  inkbot3-data:
